ReadiNow.EntityGraph
~~~~~~~~~~~~~~~~~~~~

Provides an IEntity implementation, and other services, for working on a sub-graph (interconnected network) of entities.

In particular, provides fast concurrent read-only access whereby all content of interest must be requested up-front.
