~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 XsdGen.exe
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

This utility is integrated into the build process.
The purpose of this utility is to parse the config and generate corresponding XSD schema.

Usage:
XsdGen.exe [-o result.xsd namepspace]+ [input.xml]+
Entire set of files must be self describing. Must minimally include the bootstrap.

